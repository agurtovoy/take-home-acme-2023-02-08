import Header from "@/components/header";

const Dashboard = () => {
    return (
        <main className="flex h-screen flex-col">
            <Header />
        </main>
    );
};

export default Dashboard;
